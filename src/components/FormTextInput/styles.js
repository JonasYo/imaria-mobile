import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  TextInput: {
    marginBottom: 20,
    height: 40,
    borderColor: 'gray',
    borderRadius: 4,
    color: 'gray',
    borderWidth: 1,
  },
});

export default styles;
